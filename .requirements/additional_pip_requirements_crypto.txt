# Task 2: Python Virtual Environment Setup
# ROS2 Python packages (rclpy is installed system-wide, but listed for reference)
# Note: rclpy, std_msgs, sensor_msgs, builtin_interfaces, diagnostic_msgs are ROS2 system packages

# ML Dependencies
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1
scikit-learn>=1.8.0
pandas>=2.3.3
numpy>=2.3.5
scipy>=1.16.3

# Data Collection Libraries
ccxt>=4.5.28
tweepy>=4.16.0
praw>=7.8.1
feedparser>=6.0.10  # RSS feed parsing
requests>=2.31.0  # HTTP requests for news APIs
beautifulsoup4>=4.12.0  # HTML parsing for content extraction

# Database dependencies
psycopg2-binary>=2.9.9  # PostgreSQL adapter for Python
qdrant-client>=1.7.0  # Qdrant vector database client

# Additional dependencies
pyyaml>=6.0
typeguard>=4.0.0
empy>=4.2
catkin_pkg>=0.4.0
lark>=1.0.0
cryptography>=41.0.0  # For credential encryption

# Feature Engineering & ML Dependencies
transformers>=4.35.0  # Hugging Face transformers (BERT, RoBERTa, Sentence-BERT)
sentence-transformers>=2.2.2  # Sentence-BERT models
vaderSentiment>=3.3.2  # VADER sentiment analysis
tf-keras>=2.20.0  # TensorFlow Keras compatibility for transformers
matplotlib>=3.8.0  # Chart rendering
mplfinance>=0.12.10b0  # Financial charting
Pillow>=10.0.0  # Image processing
arch>=6.2.0  # GARCH models (optional, for advanced volatility)
ta-lib>=0.4.28  # Technical analysis library (optional)
lightgbm>=4.1.0  # LightGBM gradient boosting
xgboost>=2.0.0  # XGBoost gradient boosting
optuna>=3.4.0  # Hyperparameter optimization
torch>=2.5.1  # PyTorch (already listed, but ensuring it's there)
torchvision>=0.20.1  # TorchVision (already listed)
torchaudio>=2.5.1  # TorchAudio (already listed)
